'use client';

// PwaInstallPrompt component updated:
// Automatic bottom prompt card has been removed per request.
// PWA Installation is now triggered via the Install icon in the top navbar.

export default function PwaInstallPrompt() {
  return null;
}
